// sanity.config.js
import { defineConfig } from "sanity";

export default defineConfig({
    title: "portfolio-sanity",
    projectId: "ispob9gx",
    dataset: "production",
    plugins: [],
    schema: {
        types: [],
    },
});